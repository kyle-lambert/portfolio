import React from "react";
import "./contact.scss";

import SectionGrid from "../../layout/section-grid/section-grid";

function Contact(props) {
  return (
    <div className="contact">
      <SectionGrid>contact</SectionGrid>
    </div>
  );
}

export default Contact;
